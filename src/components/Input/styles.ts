import { styled } from "@stitches/react";

export const CleanInput = styled('input', {
  maxWidth: '100%',
  height: 43,
  padding: '2px 15px',
  borderRadius: 10, 
  border: 0,
  background: "$gray50",
  marginTop: 6,
})